class ClassName
{
            declare variables
           declare constrcutors
           declare blocks

          public static void main(Strigs arg[])
           {
                   instrcutions
            }

}
=> class :
            -> class is a blueprint of an object.
            -> class contains objects, variables, methods etc.
            -> it defines the property and behvaiour of the object.
            => public : it is an access modifier .It defines the scope of any class , varibles , method, constrcutors etc, whoch means the access/limitataion of a particular entity.

=> static : static is used to defined class level varibales, methods or blocks . these static entities are defined only in the memory of the class, they are not part of any object.
=>void : it is a return type , which defines 